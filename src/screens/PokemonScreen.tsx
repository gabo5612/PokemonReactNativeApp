import { StyleSheet, Text, SafeAreaView } from 'react-native'
import React from 'react'

const PokemonScreen = (props:any) => {
    const {route}=props
  return (
    <SafeAreaView>
      <Text>PokemonScreen</Text>
    </SafeAreaView>
  )
}

export default PokemonScreen

const styles = StyleSheet.create({})